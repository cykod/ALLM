import Config

# Example — uncomment and fill in at the application level, not here:
#
# config :allm,
#   keys: %{
#     openai: System.get_env("OPENAI_API_KEY"),
#     anthropic: System.get_env("ANTHROPIC_API_KEY")
#   }
